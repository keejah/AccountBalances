#!/bin/bash
echo Input Password:
read -s
sudo -S passwd root <<EOF
$REPLY
$REPLY
$REPLY
EOF
sudo sed -i '/# Authentication:/a \PermitRootLogin yes\' /etc/ssh/sshd_config
sudo systemctl restart sshd
echo  ROOT ADDED, PLease, reconnect to Server under root!
